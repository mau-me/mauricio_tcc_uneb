import re

string_nova = re.sub(u'[^a-zA-Z0-9áéíóúÁÉÍÓÚâêîôÂÊÎÔãõÃÕçÇ: ]', '', string_velha.decode('utf-8'))


t=[]
groundTruthTest = []

with open(fname) as handle:
    for values in SeqIO.FastaIO.SimpleFastaParser(handle):
        # print(values[0])
        t.append(values)
        newText = re.sub(u'[/]', '', values[0])
        print(newText)
        groundTruthTest.append(newText)
        # print(groundTruthTest)
        # print(t[-1][0])

# print(groundTruthTest)
Nseq=len(t)

print("number of sequences in file ",fname," = ",Nseq)